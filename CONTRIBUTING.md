# 작업 가이드 (CONTRIBUTING)

영어코딩 템플릿 저장소를 관리할 때 지키는 규칙입니다.

## 폴더 구조

코스 하나는 `NNN-코스이름/` 폴더이고, 그 안에 4개의 하위 폴더가 있습니다.

- `worlds/` — 레슨별 마인크래프트 월드 파일(`.mcworld`)
- `python/` — 파이썬 코드 템플릿
- `blocks/` — 블록 코드 템플릿(`.mkcd`)
- `worksheets/` — 영어 워크시트(`.md` 원본 → `.pdf` 출력본, `_html/` 는 빌드 중간 파일)

`999-extras/` 는 특정 레슨에 묶이지 않은 단독 워크시트 모음입니다.

## 파일 이름 규칙

- 레슨 파일은 주제 이름(`agent-movement.md`)이 기본입니다. 예전 코스(002~005)는 아직 **`weekN`** (하이픈 없음): `week1.mcworld`, `week2.md` …
- 여러 레슨이 공유하는 월드는 하나로: `week4-5.mcworld`, `week6-7-8.mcworld`
- (구버전 `week-1` 하이픈 표기는 `weekN` 으로 통일했습니다.)

## 새 레슨 / 워크시트 추가

1. `scripts/templates/` 에서 알맞은 템플릿을 복사합니다.
   - 일반 워크시트 → `week-template.md`
   - 단어 연습 → `vocab-template.md`
2. 내용을 채워 해당 코스의 `worksheets/weekN.md` 로 저장합니다.
3. `python scripts/build-worksheets.py` 로 PDF를 만듭니다.
4. 코스 `README.md` 표(또는 `999-extras` 목록)에 받기 링크를 추가합니다.

## 코스 README 표준 형식

마인크래프트 코스(`000`~`004`)의 `README.md` 는 아래 순서를 그대로 따릅니다. 새 코스를 만들 때도 이 형식으로 시작하세요.

```
# <이모지> <코스 이름>                        ← 번호 없이 이름만. 보관 코스는 뒤에 *(보관 · 서비스 종료)*
(보관된 코스만) > 📦 **보관된 코스입니다.** … 대체 코스 링크

<코스 한두 줄 소개>                          ← "N주 동안", "N번째 코스" 같은 표현 금지

> ⏱️ 정해진 기간은 없습니다. 아래 순서대로, 아이가 준비되면 다음 레슨으로 넘어갑니다.

---

## 📚 레슨 순서
| 레슨 | 주제 | 📦 월드 | 🎬 영상 … | 📝 워크시트 … |     ← 첫 칸은 "레슨", 절대 "주"가 아님
> 표 아래 안내 문구 (공유 월드, 워크시트 단계 등)

---

## 🧭 배우는 순서                              ← 선택. 개념 흐름을 보여주고 싶을 때만

---

## 📂 자료
- 🗂️ **전체 파일 저장소**: GitHub 코스 폴더 링크
- 📦 `worlds/` — 레슨별 월드 파일
- 📝 `worksheets/` — 레슨별 영어 워크시트 (단계 표기)
각 자료는 **노션 숙제 페이지** 에서도 …

---

## 🎯 다음 코스
이 코스를 마치면 [다음 코스](../NNN-…) 로 이어집니다.
```

규칙 몇 가지:

- **주차 표현 금지.** "N주차 / 8주 / 매주"는 쓰지 않습니다. 레슨은 번호로만 부르고, 진행 기간은 정하지 않습니다.
- **순서 번호(첫 번째·두 번째 코스) 금지.** 코스가 바뀌면 바로 틀려집니다. 무엇을 배우는지로 설명하세요.
- **파일이 없으면 링크 대신 `*준비 중*`.** 실제 파일이 아닌 "준비 중" 텍스트 파일을 링크로 걸지 않습니다. (`.mcworld` 1KB 미만, `.mkcd` 500B 미만이면 실파일이 아닙니다.)
- **코스 진행 순서**: `000-builder-basics` → `001-maze-madness` → `002-3d-coordinates` → `003-functions-games` → `004-theme-park-creator`.
- **코스 번호는 폴더 이름에만 씁니다.** 학부모·학생이 보는 제목(README H1, 워크시트, 슬라이드, 노션)에는 번호를 넣지 않고 코스 이름만 씁니다.
- 보관된 코스는 번호를 떼고 `archived-<이름>` 으로 옮깁니다. (예: `archived-pyramid-problems` — 내용은 빌더 기초 레슨 11~15)
- 노션 커리큘럼 페이지와 이 저장소가 다르면 **저장소가 기준**입니다.

## 언어 규칙

- **학생용**(워크시트 본문) = 영어. 과하게 규정하지 않기, "최소 N개" 같은 강제 표현 금지.
- **학부모용**(코스/폴더 README, 안내문) = 한국어. 브랜드는 "영어코딩".
- 제출은 항상 **온라인 카카오톡**. "수업에 가져오기" 같은 오프라인 표현 금지.

## 커밋

- 학부모가 읽을 수 있는 한국어 커밋 메시지. `feat:` 같은 접두사는 쓰지 않습니다.
- 마이그레이션/SQL 은 한 커밋에 하나의 변경만.

## 선생님 전용 파일

`worlds/_gen_*.py` · `_builder_*.py` · `_*_lib.py` 는 월드 생성기로, `.gitignore` 에 의해 배포에서 제외됩니다. 학생에게는 최종 `.mcworld` 만 전달됩니다.
